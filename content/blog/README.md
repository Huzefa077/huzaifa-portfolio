# Blog posts

Add locally published blog posts to this directory as Markdown files. The
filename becomes the post slug; for example, `my-first-post.md` becomes
`/blog/my-first-post/`.

External articles recommended from other websites belong in
`src/data/blog.ts` instead.
